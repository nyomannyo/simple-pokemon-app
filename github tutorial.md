pertama kali setelah create repository baru di github

git init

git remote add origin https://github.com/nyomannyo/simple-pokemon-app.git





==================================================================================

ketika ada perubahan di project local dan ingin di-push ke repository

git pull

git add .

git commit -m 'pesan commit'

git push

git status